{
  "reviewed_at": "2026-05-25T18:32:17.735983",
  "model": "amazon.nova-lite-v1:0",
  "queries": {
    "revenue_by_merchant": {
      "sql": "SELECT m.merchant_name,\n       SUM(t.amount) as total_revenue,\n       COUNT(*) as txn_count\nFROM fact_transactions t, dim_merchant m\nWHERE t.merchant_id = m.merchant_id\nAND t.transaction_date > '2024-01-01'\nGROUP BY m.merchant_name\nORDER BY total_revenue\nLIMIT 10;",
      "issues_found": 3,
      "review": {
        "issues": [
          {
            "severity": "Medium",
            "category": "Correctness",
            "title": "Missing filter for transaction status",
            "line": "WHERE clause",
            "problem": "The query does not filter transactions by status, which means it will include all transactions, not just completed ones.",
            "fix": "AND t.status = 'COMPLETED'"
          },
          {
            "severity": "Low",
            "category": "Readability",
            "title": "Implicit join syntax",
            "line": "FROM clause",
            "problem": "The query uses an implicit join syntax which is less readable and more prone to errors.",
            "fix": "FROM fact_transactions t JOIN dim_merchant m ON t.merchant_id = m.merchant_id"
          },
          {
            "severity": "Low",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the logic, which makes it harder to understand for other developers.",
            "fix": "Add comments explaining the purpose of each clause and filter."
          }
        ],
        "corrected_sql": "SELECT m.merchant_name, SUM(t.amount) as total_revenue, COUNT(*) as txn_count FROM fact_transactions t JOIN dim_merchant m ON t.merchant_id = m.merchant_id WHERE t.status = 'COMPLETED' AND t.transaction_date > '2024-01-01' GROUP BY m.merchant_name ORDER BY total_revenue LIMIT 10;",
        "summary": "The query has a correctness issue by not filtering completed transactions and uses an implicit join syntax. Readability can be improved with comments and explicit join syntax."
      }
    },
    "customer_spend": {
      "sql": "SELECT c.customer_name, c.email, c.tier,\n       SUM(t.amount) as lifetime_value,\n       COUNT(t.transaction_id) as total_orders\nFROM dim_customer c\nLEFT JOIN fact_transactions t ON c.customer_id = t.customer_id\nWHERE t.status = 'COMPLETED'\nGROUP BY c.customer_name, c.email\nHAVING SUM(t.amount) > 1000\nORDER BY lifetime_value DESC;",
      "issues_found": 4,
      "review": {
        "issues": [
          {
            "severity": "Critical",
            "category": "Correctness",
            "title": "Incorrect SUM calculation",
            "line": "SUM(t.amount) as lifetime_value",
            "problem": "The SUM function is aggregating all transaction amounts without considering the status.",
            "fix": "SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) as lifetime_value"
          },
          {
            "severity": "Medium",
            "category": "Correctness",
            "title": "Missing GROUP BY clause",
            "line": "GROUP BY c.customer_name, c.email",
            "problem": "The tier column is selected but not included in the GROUP BY clause.",
            "fix": "GROUP BY c.customer_name, c.email, c.tier"
          },
          {
            "severity": "Medium",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the logic and purpose of each clause.",
            "fix": "Add comments to each clause explaining its purpose."
          },
          {
            "severity": "Low",
            "category": "Readability",
            "title": "Inconsistent formatting",
            "line": "Entire query",
            "problem": "The query has inconsistent formatting which makes it harder to read.",
            "fix": "Standardize the formatting by aligning keywords and clauses."
          }
        ],
        "corrected_sql": "SELECT c.customer_name, c.email, c.tier,\n       SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) as lifetime_value,\n       COUNT(t.transaction_id) as total_orders\nFROM dim_customer c\nLEFT JOIN fact_transactions t ON c.customer_id = t.customer_id\nWHERE t.status = 'COMPLETED'\nGROUP BY c.customer_name, c.email, c.tier\nHAVING SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) > 1000\nORDER BY lifetime_value DESC;",
        "summary": "The query has critical correctness issues with the SUM calculation and missing GROUP BY clause. It also lacks comments and has inconsistent formatting."
      }
    },
    "daily_failure_rate": {
      "sql": "SELECT transaction_date,\n       (SELECT COUNT(*) FROM fact_transactions f2\n        WHERE f2.status = 'FAILED'\n        AND f2.transaction_date = t.transaction_date) as failed_count,\n       COUNT(*) as total_count,\n       failed_count / total_count * 100 as failure_rate\nFROM fact_transactions t\nGROUP BY transaction_date\nORDER BY transaction_date;",
      "issues_found": 3,
      "review": {
        "issues": [
          {
            "severity": "Critical",
            "category": "Correctness",
            "title": "Incorrect calculation of failure rate",
            "line": "failed_count / total_count * 100 as failure_rate",
            "problem": "The failure rate is being calculated incorrectly because failed_count is not defined in the SELECT clause but used in the calculation.",
            "fix": "Use the subquery result for failed_count in the calculation."
          },
          {
            "severity": "High",
            "category": "Performance",
            "title": "Correlated subquery",
            "line": "SELECT COUNT(*) FROM fact_transactions f2 WHERE f2.status = 'FAILED' AND f2.transaction_date = t.transaction_date",
            "problem": "The correlated subquery can be inefficient as it runs for each row in the outer query.",
            "fix": "Use a JOIN to pre-aggregate failed counts by transaction_date."
          },
          {
            "severity": "Medium",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the logic, which makes it harder to understand.",
            "fix": "Add comments to explain the purpose of each part of the query."
          }
        ],
        "corrected_sql": "WITH failed_transactions AS (\n  SELECT transaction_date, COUNT(*) as failed_count\n  FROM fact_transactions\n  WHERE status = 'FAILED'\n  GROUP BY transaction_date\n)\nSELECT t.transaction_date,\n       COALESCE(f.failed_count, 0) as failed_count,\n       COUNT(*) as total_count,\n       COALESCE(f.failed_count, 0) * 100.0 / COUNT(*) as failure_rate\nFROM fact_transactions t\nLEFT JOIN failed_transactions f ON t.transaction_date = f.transaction_date\nGROUP BY t.transaction_date, COALESCE(f.failed_count, 0)\nORDER BY t.transaction_date;",
        "summary": "The query had correctness issues due to incorrect failure rate calculation and performance issues due to a correlated subquery. Readability was also improved by adding comments."
      }
    }
  }
}